[package]
name = "tyde-process-env"
edition.workspace = true

[dependencies]
tokio.workspace = true
tracing.workspace = true

[target.'cfg(unix)'.dependencies]
rustix = { version = "1", features = ["process"] }
